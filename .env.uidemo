# Loaded by `npm run ui` (vite --mode uidemo).
# Runs the interface on sample data with no Firebase and no login, so the look
# can be worked on without credentials. Contains nothing secret, and the flag is
# ignored by any production build — see src/lib/preview.ts.
VITE_PREVIEW=1
